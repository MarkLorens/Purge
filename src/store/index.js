import { createStore } from 'vuex'
import auth from './auth'
import issue from './issue'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth,
    issue
  }
})
